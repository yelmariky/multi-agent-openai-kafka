apiVersion: v1
kind: ConfigMap
metadata:
  name: audit-agent-config
  namespace: multi-agent
data:
  KAFKA_BOOTSTRAP_SERVERS: "kafka.agent-system.svc.cluster.local:9092"
  AUDIT_INPUT_TOPIC: "audit.events.in"
  AUDIT_OUTPUT_TOPIC: "audit.events.out"
  AUDIT_GROUP_ID: "audit-agent"
