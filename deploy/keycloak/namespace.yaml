# Namespace dédié à Keycloak — isolé du reste de la plateforme
apiVersion: v1
kind: Namespace
metadata:
  name: keycloak
  labels:
    app.kubernetes.io/part-of: ia-insight-platform
    istio-injection: enabled
