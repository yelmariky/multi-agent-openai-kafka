# PostgreSQL credentials — copier en .env et remplir avant d'exécuter install.sh
# NE PAS committer ce fichier avec les vraies valeurs

POSTGRES_USER=pgadmin
POSTGRES_PASSWORD=changeme
POSTGRES_DB=iainsight
